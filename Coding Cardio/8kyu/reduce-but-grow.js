// Given a non-empty array of integers, return the result of multiplying the values together in order. Example:

// [1, 2, 3, 4] => 1 * 2 * 3 * 4 = 24

arr1 = [1, 2, 3, 4]; //expect 24
arr2 = [2, 3, 5]; //expect 30

function grow(...args) {
	console.log(args);
}

grow('first', 'second', 'third');
